# todo_list 视觉与交互规范

> 最后更新：2026-09-17

## 产品界面原则

- 模式：Operate。优先保证手机端快速扫描、单手操作和明确反馈。
- 使用 Material 3 原生组件与当前主题色，不引入独立装饰性视觉体系。
- 任务卡保持轻量；同一信息只出现一次，空状态不占据大块屏幕。
- 可点击图标使用一致图标库，触控区域不小于 48dp。

## 安排页面

- 手机底部导航固定为“安排 / 备忘录”；首次进入默认打开安排的本周，重开后按语义值恢复上次根页面。
- 安排顶部只保留一层“本周 / 阶段 / 收件箱”，三项等宽且一次只显示一个页面。
- 手机安排页采用已确认的 **方案 B：紧凑工作区**：第一行显示“安排”、同步状态和更多；第二行显示“本周 / 阶段 / 收件箱”。参考图为 `docs/assets/安排页方案B-紧凑工作区.png`。
- 顶部导航下方使用三列紧凑摘要卡：今日待办、当前查看周待办、当前查看周完成。摘要服务于快速判断负荷，不使用大号欢迎语、装饰插画或占据首屏的英雄卡片。
- 删除固定 `Todo List` 标题和“列表 / 安排”“阶段 / 本周”双层切换。
- 本周工作区依次为摘要卡、周导航、状态筛选、星期任务组。周导航的上一周、日期范围、调整日期、下一周保持单行；窄屏优先压缩间距，不允许文字或按钮越界。
- 使用暖白/中性表面与低饱和靛蓝强调色；卡片圆角 14–16dp、细描边、轻层级，保持紧凑但不拥挤。
- 阶段视图保留“现在 / 接下来 / 稍后”；本周视图使用同款星期分组。
- 新建入口使用分组底部 48dp 整行按钮：阶段为“＋ 同时任务”，本周为“＋ 添加到今天/周几”。标题不放加号，安排页不显示右下角悬浮新建按钮。
- 分组快捷添加只输入任务名称，自动绑定阶段或日期；完整属性在任务详情中继续编辑。
- 任务操作使用始终可见的 `more_vert`，任务标题和拖拽柄不能挤掉该入口。
- 应用内三点入口统一为 22dp 图标、至少 48dp 触控区；菜单顺序为编辑、移动/同步、分隔线、删除。
- 手机分组允许独立收起；180ms 尺寸动画沿用既有行为。
- Android 阶段任务长按整行只做组内排序；跨阶段、移回收件箱与同步今日通过三点菜单。

## 收件箱职责

- 筛选与统计面板中的智能视图固定按“收件箱 / 已安排 / 已完成 / 最近删除”横向排列，不再显示精灵窝。
- 收件箱只显示未完成且没有阶段、没有日期的任务；已安排只显示有阶段或日期的未完成任务。
- 已安排是平铺管理页，只提供详情、编辑、调整阶段和日期，不显示完成与删除入口。
- 移除“今天 / 未来七天”智能视图；日期计划统一进入“安排 > 本周”。
- Android 收件箱长按整张任务卡排序；多选只能通过明确的“选择”按钮进入。
- 收件箱三点菜单固定为“编辑 / 移到阶段 / 移到本周 / 分隔线 / 删除”。
- 最近删除任务使用实际删除日期，不显示没有自动清理支撑的剩余天数。

## 状态视图与恢复

- 阶段和本周的筛选固定为“未完成 / 已完成 / 最近删除”；收件箱使用“收件箱 / 已安排 / 已完成 / 最近删除”四个智能视图。
- 收件箱、已安排、已完成、最近删除严格互斥，主任务区一次只渲染当前状态。
- 完成和删除是全局状态，但归档入口按操作来源 `inbox/stage/week` 分开显示。
- 从收件箱已完成恢复时回收件箱；从阶段恢复时保留阶段；从本周恢复到设备当前周相同星期几并保留阶段。
- 最近删除中的已完成任务恢复后回到对应已完成视图，不重新安排提醒。

### 最近删除卡片

- 使用专用 `DeletedTaskCard`，不得复用普通任务完成卡片。
- 标题最多两行；来源与删除时间用次级信息展示。
- 恢复为始终可见的描边按钮，永久删除位于三点菜单并使用危险色。
- 触控目标至少 48dp；320/360dp 将操作区换到卡片底部，412dp 及更宽可放在右侧。
- 不使用复选框、删除线、分类标签、安排标签、子任务进度或拖动柄。

## 本周信息密度

- 周一为每周起点，按设备本地日期计算“今天”。
- 周一至周日七个标题按自然顺序固定显示，不允许交换日期标题，也不从今天重新计算七天。
- 星期标题使用“周四 · 9/17”短格式；月份与日期保留，但不重复使用“月/日”长文案挤占横向空间。
- Android/窄屏打开当前周时，以今天的日期卡作为滚动起点，首屏直接显示今天任务；摘要、周导航和过去日期保留在上方，可向上查看。Windows 宽屏保持从周首开始。
- 切换到上周或下周时从周首开始；从其他周返回本周时重新定位今天，从任务详情返回时保留离开前的滚动位置。
- 今天没有任务时仍默认展开并显示当天快捷添加入口。
- 空日期压缩成 48dp 单行，不显示 `0`；展开后显示该日新建入口。
- 今天和未来日期只显示未完成任务；过去日期展开后同时显示该日未完成任务和从本周完成的记录，阶段或收件箱完成的记录不得混入。
- 已过去且没有未完成任务的日期默认收起；若有本周完成记录，标题状态显示“已完成”。存在未完成任务时自动展开。
- 同一天内长按任务排序；点击“调整日期”后，七个日期均可接收未完成任务，放下只改日期和提醒。
- 本周不是月历：禁止月历、滚动日期条和时间轴。
- 提供上一周 / 本周 / 下一周导航，避免移除“未来七天”后无法检查周边计划。
- 本周今日优先的确认方向稿为 `docs/assets/本周今日优先方案.png`，实际组件验收图为 `docs/assets/本周今日优先实际组件验收.png`。

## 列表与页面恢复

- Todo 列表取消滑动完成；只允许向左滑出操作，避免无意完成任务。
- Android 保存根页面、安排的本周/阶段/收件箱及当前智能视图。
- 重启恢复本周时始终回到设备当前周；不恢复详情页、搜索字词和临时筛选状态。

## 子任务详情页

- 顶部只显示任务标题与“编辑”；完成进度统一放在正文首张进度卡，不重复展示统计。
- 进度卡使用低饱和容器色、16dp 圆角和细描边，显示“还剩 N 项 / 全部完成”、`完成数 / 总数` 与进度条。
- 子任务使用 14dp 圆角紧凑卡片、圆形完成按钮和最多两行标题；日期作为次级信息，逾期使用主题危险色。
- 同级子任务长按排序；三点菜单不显示上移 / 下移，只提供编辑、添加下级、提升一级、设为上一项的下级、分隔线、删除。
- 层级缩进为 20dp；展开箭头、三点入口和完成按钮的触控区均不小于 48dp。
- 底部添加栏放入系统安全区，使用填充输入框与 50dp 圆形添加按钮。
- 空状态使用原生 `checklist` 图标和一句说明；禁止用 Emoji 充当操作图标。
- 参考方向稿为 `docs/assets/子任务详情页统一方案.png`，实际组件基准为 `test/goldens/subtask_detail_mobile.png`。

## 备忘录页面

- 顶部显示当前分类、搜索、同步和更多，不显示重复的大号“备忘录”标题。
- 备忘录卡片保持纯文本与日期/分类层级，不加入任务复选框、阶段或本周控件。
- 三点菜单固定为“编辑 / 置顶 / 归档 / 转为任务 / 关联任务 / 分隔线 / 删除”，使用 20dp 线性图标。

## Android 主屏幕小部件

- 手机和平板提供两个独立小部件：“现在”与“本周”；均可上下滚动，不截断任务数量。
- 标题右侧保留独立加号；只输入标题，“现在”自动加入现在，“本周”自动加入今天。
- 任务行可直接完成；有子任务时显示 `完成数/总数` 并展开一级子任务，同一小部件一次只展开一个任务。Android 24–30 使用兼容的勾选图标按钮，不在远程视图里使用仅 API 31+ 支持的 `CheckBox`。
- 三点菜单提供编辑、软删除和移动阶段；小部件不提供拖拽排序，也不展示深层子任务。
- 小部件与 Flutter 应用共用 `todo_list.db`，不得建立第二份任务数据。

## App 图标

- Android 使用已确认的 A6“六序镜花”：深墨蓝底、六个分离的棱镜模块、铂银与冷灰蓝切面、中心六边形完成标记。
- 六向结构用于表达秩序、推演和万花筒折射；模块必须分离，禁止画成两个完整三角形叠加的标准六芒星。
- Android 8.0+ 使用自适应前景/背景，主体保留在安全区；Android 13+ 提供单色主题图标；旧系统保留五档普通与圆形 PNG。
- 图标不得使用文字、高饱和彩虹渐变、卡通花瓣、十字架或复杂阴影。
- 设计母版为 `docs/assets/App图标-A6六序镜花.svg`，圆形母版为同名 `-圆形.svg`。

## 反馈

- 成功与失败反馈使用可关闭的浮动 SnackBar。
- 排序不提供撤销；失败时重新读取本地顺序并明确说明恢复结果。
- 提示不能遮挡主要操作入口或系统安全区。

## Android 更新界面

- 更新入口位于应用级“更多”菜单，使用 `system_update_alt_outlined` 线性图标，不占用安排页主导航。
- 更新弹窗沿用 Material 3 `AlertDialog`：版本和大小为次级信息，更新内容为正文，下载/安装为唯一主按钮。
- 启动自动检查保持安静，发现新版才弹窗；手动检查必须立即显示进行中，并明确显示最新、超时或失败结果。
- 更新始终允许“稍后”和返回关闭；最低兼容版本只提高提示强度，不锁住应用。
- 下载中使用确定进度条并显示百分比；取消下载始终可见。失败信息使用主题危险色，并把主按钮改为“重新下载”。
- 需要未知来源权限时使用明确的系统设置引导；不伪装成应用内静默安装。
- 320dp 宽度下标题、更新内容、按钮和进度信息不得越界；长更新说明在弹窗内容区滚动。

## 禁止项

- 不把“现在 / 接下来 / 稍后”称为优先级。
- 不复制任务来实现阶段与本周双视图。
- 不把日期写进 `task_mode`；阶段来自 `task_mode`，本周来自 `due_date`。
