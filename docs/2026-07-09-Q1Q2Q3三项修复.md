# 2026-07-09 Q1/Q2/Q3 三项修复

## Q1: 通知队列

**文件**: `lib/services/notification_service.dart` + `lib/main.dart`

**修改**:
- `pendingNotification` 从单值 `T?` → `List<T>` 队列
- Timer 回调 append 到队列
- `_showPending` 加 `_dialogShowing` 防重入，弹窗关闭后自动消费下一条

---

## Q2: Todo 卡片右键菜单

**文件**: `lib/widgets/todo_item.dart`

**修改**: Card 的 `InkWell` 加 `onSecondaryTapUp` → `showMenu`（编辑/删除/上移/下移）。

和备忘录一致的交互：左键进详情，右键弹出操作菜单，3-dot 按钮保留。

---

## Q3: 子任务闹钟

**文件**: `lib/screens/task_detail_screen.dart` + `lib/screens/todo_screen.dart`

**修改**:
- `TaskDetailScreen` 新增 `NotificationService` 依赖
- 新增 `_scheduleSubTaskReminder()` / `_cancelSubTaskReminder()`（通知 ID: `st.id + 30000`）
- `_addChild()`: insert 后调度
- `_editSubTask()`: 编辑前取消旧 → 保存后调度新
- `_deleteSubTask()`: 删除时取消
- `_restoreSubTask()`: 恢复时调度
- `todo_screen.dart` 传 `notificationService` 给详情页
