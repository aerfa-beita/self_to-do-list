# 2026-07-09 子任务行 hover 重写

## 状态：⚠️ 未解决，已跳过

两轮尝试均未生效，原因不明，待后续排查：
1. 第一轮：`Material(transparent) + InkWell(hoverColor)` → hover 不可见
2. 第二轮：`MouseRegion` + `Container` 背景色切换 → 仍未生效

## 已做改动

`lib/widgets/todo_item.dart`：
- `_subTaskRow()` 方法 → `_SubTaskRowWidget` StatefulWidget
- `MouseRegion` + `Container` 背景色 hover
- 右键菜单 + 左键 toggle checkbox
