# 2026-07-09 完善与重构

## 概述

对 to-do_list 项目进行 8 项完善：修 Bug、补功能、去重复、建文档。

---

## 变更清单

### 1. 项目基建文档
- 新建 `project-docs/INDEX.md` — 项目索引、技术栈、架构概览
- 新建 `project-docs/STRUCTURE.md` — 目录树、文件职责、分层架构图、数据流
- 新建 `project-docs/CODE_STATS.md` — 20 文件 / 3,458 行代码统计、重复代码标记

### 2. Bug 修复：AddTaskDialog 标题
- **文件**: `lib/widgets/add_todo_dialog.dart:90`
- **修改**: "新建备忘录"/"编辑备忘录" → "新建任务"/"编辑任务"
- **原因**: dialog 用于 Todo 任务，标题写"备忘录"是文案错误

### 3. 备忘录通知接入
- **文件**: `lib/screens/memo_screen.dart`, `lib/main.dart`
- MemoScreen 新增 `NotificationService` 依赖
- 新增 `_scheduleMemoReminder()` / `_cancelMemoReminder()` 方法
- `_editMemo` 保存后自动调度/取消提醒（通知 ID: `memo.id + 20000`）
- `_softDelete` 删除时取消提醒，`_restore` 恢复时重新调度
- main.dart 传入 notificationService

### 4. 搜索功能
- **文件**: `lib/screens/todo_screen.dart`, `lib/screens/memo_screen.dart`
- Todo 列表：搜索栏在分类 chips 下方，按标题+备注搜索
- 备忘录列表：搜索栏在分类 chips 下方，按内容搜索
- 支持一键清除搜索词

### 5. 深色模式
- **文件**: `lib/main.dart`
- 新增全局 `themeModeNotifier` (ValueNotifier)
- 用 `ValueListenableBuilder` 包裹 MaterialApp 实现实时切换
- AppBar 右侧太阳/月亮图标一键切换
- 亮色/暗色两套 ThemeData（Material 3 + indigo seed）

### 6. 备忘录 UI 完善
- **文件**: `lib/widgets/memo_item.dart`, `lib/screens/memo_screen.dart`
- MemoItem 卡片新增分类 chip（与 TaskItem 一致的样式）
- MemoItem 新增 `_chip()` / `_categoryColor()` 方法
- 备忘录列表新增"管理分类"按钮 → `_showCategoryManager()`
- 8 个默认分类不可删除/重命名（`isDefault = i < 8`）

### 7. 代码去重
- **文件**: `lib/utils/date_utils.dart`, `lib/widgets/todo_item.dart`, `lib/widgets/memo_item.dart`
- `categoryColor()` 函数抽取到 `date_utils.dart`，两处调用统一
- 删除 `todo_item.dart:286` 和 `memo_item.dart` 的重复 `_categoryColor`
- **新建** `lib/widgets/date_time_picker.dart` — 复用日期+时间选择器组件（供后续使用）

### 8. 数据导出
- **文件**: `lib/database/database.dart`, `lib/main.dart`
- `DatabaseProvider.exportAllJson()` — 读取 5 张表全量数据，输出格式化 JSON
- 导出路径: `Documents/todo_list_backup_<时间戳>.json`
- AppBar 新增"📤 导出数据 (JSON)"菜单项
- 成功后弹窗显示文件路径（可选中复制）

---

## 分析结果

```
dart analyze lib/
5 issues found (all info-level, no errors or warnings)
  - 4x use_build_context_synchronously (预存，非本次引入)
  - 1x unnecessary_underscores (预存)
```

---

## 待后续

- Screen 文件仍偏大（3 个文件 > 400 行），可继续拆分
- `use_build_context_synchronously` lint 可统一处理
- 日期选择器 inline 代码可改用 `DateTimePicker` 组件
- 缺少单元测试覆盖
