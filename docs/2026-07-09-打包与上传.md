# 2026-07-09 打包与上传

## Windows Release

`flutter build windows` → `build/windows/x64/runner/Release/todo_list.exe`

- 解决中文路径乱码：项目从 `D:\实践项目` 移至 `D:\MY_Project`
- 删 CMake 缓存（旧路径残留）
- Release 文件夹 ~32MB，发朋友直接 zip 即可

## Android APK

`flutter build apk --release` → `build/app/outputs/apk/release/app-release.apk` (58.6MB)

- 阿里云 HTTP 镜像加速 Gradle 依赖下载
- Gradle 本地 zip（`gradle-wrapper.properties` 指向本地）
- 启用 `isCoreLibraryDesugaringEnabled`（flutter_local_notifications 要求）
- NDK 28.2 自动重装

## 构建配置变更

| 文件 | 变更 |
|------|------|
| `android/build.gradle.kts` | 恢复 google() + mavenCentral() |
| `android/gradle/wrapper/gradle-wrapper.properties` | 本地 file:/// zip |
| `android/app/build.gradle.kts` | coreLibraryDesugaring + desugar_jdk_libs |
| `windows/runner/main.cpp` | 窗口标题 → L"todo_list"（可改） |

## GitHub 状态

- `git init` + commit 90 files
- 用户需创建 GitHub repo 并 push
