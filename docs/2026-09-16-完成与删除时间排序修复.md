# 完成与删除时间排序修复

> 日期：2026-09-16
> 数据库：v22，无迁移

## 改动

- 已完成视图按 `completed_at` 从新到旧排序。
- 最近删除视图按 `deleted_at` 从新到旧排序。
- 时间相同时按 `updated_at`、任务 ID 从新到旧稳定排序。
- 排序在 inbox/stage/week 来源过滤之后执行，不改变来源隔离。

## 验证

- 增加完成与删除视图的界面顺序回归测试。
- `git diff --check` 通过。
- Flutter/Dart 命令在当前 Codex 环境启动阶段无输出，需在普通 PowerShell 补跑完整测试。

## 维护提示

历史状态列表不能沿用任务的手动 `sort_order`；完成和删除视图应以事件发生时间为主序。
