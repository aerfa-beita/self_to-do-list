# 备忘录 + Todo List — 交接文档

> 最后更新：2026-07-09 | 所有 Phase 完成

---

## 一、环境

| 工具 | 路径 | 版本 |
|------|------|------|
| Flutter SDK | `D:\EXE_Download\flutter` | 3.44.5 |
| Android Studio | `D:\EXE_Download\android` | — |
| Android SDK | `C:\Users\yjhdetianxuan\AppData\Local\Android\Sdk` | — |
| 项目 | `D:\实践项目\to-do_list` | — |

**代理**：梯子开 TUN 模式，否则 Flutter 拉不了包。

---

## 二、快速启动

```bash
cd D:\实践项目\to-do_list
flutter run -d windows        # 开发运行
dart analyze lib\             # 代码检查（3秒）
flutter build windows         # 打包
```

**快捷键**：`Ctrl+N` 在 Todo Tab 新建任务

---

## 三、代码结构（v14）

```
lib/
├── main.dart                      # DI 链 + TabBar
├── models/                        # 纯数据类
│   ├── task.dart, sub_task.dart, memo.dart
├── database/
│   └── database.dart              # DatabaseProvider + v14 迁移
├── repositories/                  # 纯 SQL
│   ├── task_repository.dart
│   ├── subtask_repository.dart
│   ├── category_repository.dart
│   ├── memo_repository.dart
│   └── memo_category_repository.dart
├── services/                      # 业务编排
│   ├── task_service.dart, memo_service.dart, notification_service.dart
├── screens/
│   ├── todo_screen.dart           # Todo 主页（514行）
│   ├── memo_screen.dart           # 备忘录主页（464行）
│   └── task_detail_screen.dart    # 任务详情（516行）
├── widgets/
│   ├── todo_item.dart, memo_item.dart, add_todo_dialog.dart
└── utils/
    └── date_utils.dart
```

**架构**：UI → Service → Repository → Database（单向依赖，构造函数注入，零全局单例）

---

## 四、已完成功能

树形嵌套、三区视图、时间提醒、排序（主+子+备忘）、软删除（15天保留+子项划线移底）、8类备忘录分类、右键菜单。详见 CLAUDE.md。

---

## 五、已知问题

| 问题 | 说明 |
|------|------|
| Windows 通知 | Timer + ValueNotifier → MainScreen AlertDialog 弹窗。app 关闭时不通知。 |
| 闹钟需要重启 | 数据库直插的测试任务需重启 app 才被 `_loadAll` 调度 Timer |
| hook 中文乱码 | PS 5.1 + UTF-8 无 BOM 导致。已改为 PS 纯 ASCII + JSON 文件方案，`rules.json` 存中文 |
| 三个 screen 偏大 | todo/memo/detail 都在 450-520 行，等加第三种树形需求时再抽通用 TreeView |
| `_buildTree` 重复 | 等 Rule of Three 触发时再抽象 |

---

## 六、项目文档体系

| 文件 | 用途 | 谁读 |
|------|------|------|
| `CLAUDE.md` | 项目总览：功能清单、技术栈、进度 | AI |
| `交接文档.md` | 环境、坑、快速启动 | 接手的 AI/人 |
| `project-docs/INDEX.md` | 所有操作记录索引 | AI |
| `project-docs/STRUCTURE.md` | 目录结构 | AI |
| `project-docs/CODE_STATS.md` | 代码行数统计 | AI |
| `project-docs/docs/` | 每次操作的技术文档 | AI |
| `HANDOVER.md` | 已删除（合并到本文件） | — |

## 七、下一步

暂无待办。有新需求直接加。
